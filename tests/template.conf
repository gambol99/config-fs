$TEMPLATE$
{{ set_key_base("base","/product/web/config") ) }}
database:
  host: {{ service('consul','prod_database').Hostname }}
  password: {{ getv("base",'/db/password') }}

# the hosts for web frontend
web_frontend:
{{ for index, service := range service("service","frontend_http") }}
  web{{ index }}: {{ .Hostname }}:{{ .Port }}
{{ end }}
